#!/usr/bin/env bash
# bin/detect <build-dir>

#if [[ $(ls $1/*.rockspec 2> /dev/null | wc -l) -gt 0 ]]; then
#if [[ $(ls $1/*.rockspec 2> /dev/null | wc -l) -gt 0 ]]; then
#  echo "VLC" && exit 0
if [[ -f $1/bin/vlc ]]; then
  echo "Lua-legacy" && exit 0
else
  echo "no" && exit 1
fi
